# E2E Final Report

Date: 2026-04-17
Scope: Durability mini-suite for restart recovery (`approval-recovery`, `clarification-recovery`, `mutation-dedupe-recovery`)
Overall result: 3 passed

| Case | Success | Turns | Perceptions | Traces | Prompt used |
| --- | --- | ---: | ---: | ---: | --- |
| `approval-recovery` | Yes | 0 | 0 | 0 | `Synthetic approval seed: create_tab("https://example.test/?store=techdirect")` |
| `clarification-recovery` | Yes | 0 | 0 | 0 | `Synthetic clarification seed: "Two workspaces match. Which one should I continue with?"` |
| `mutation-dedupe-recovery` | Yes | 3 | 3 | 1 | `Open the TechDirect store link in a new tab and stop once the store page is open.` |

## Metric Definitions

- `Turns`: total recorded trace turns across the trace file(s) for that case.
- `Perceptions`: turns where the trace input included `Page Interpretation`.
- `Traces`: number of trace sessions produced for that case, including replans or retries.
- `Success`: whether the case completed successfully in the run.

## Stability Notes

- `approval-recovery` and `clarification-recovery` are now harness-driven. They exercise real checkpoint restore and side-panel rehydration, but they intentionally produce no agent trace because they do not invoke the planner/executor models.
- `mutation-dedupe-recovery` remains the slowest case and the only model-driven member of this mini-suite. It passed on the latest run without replaying `create_tab` after restart.
