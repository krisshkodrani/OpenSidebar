# Unified Agent Mode

> **Note:** Speed mode was merged into a single unified mode in Feb 2026. See [Agent Loop Architecture](../architecture/agent-loop.md#unified-mode-behavior) for the current behavior.

The agent now always uses the unified mode which combines the best behaviors from the former speed and normal modes: parallel tool execution, modal auto-dismiss, real-time streaming, and dynamic context compression.
