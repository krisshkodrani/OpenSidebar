# Eval Critique Report

Generated: 2026-02-26T10:10:24.263Z

## Summary

| Metric | Value |
|--------|-------|
| Total cases | 10 |
| Pass rate | 10.0% (1/10) |
| Failed | 9 |
| Errors | 0 |
| Avg tool name match | 0.200 |
| Avg param match | 0.100 |
| Avg sequence match | 0.101 |

### Judge Scores (10 cases judged)

| Dimension | Avg Score |
|-----------|-----------|
| Tool Selection | 3.4/10 |
| Parameter Accuracy | 4.3/10 |
| Efficiency | 3.3/10 |
| Anti-Pattern Avoidance | 3.8/10 |
| Reasoning Quality | 3.1/10 |

## Per-Pathology Breakdown

| Pathology | Pass Rate | Fails | Errors | Avg ToolSel | Avg AntiPat |
|-----------|-----------|-------|--------|-------------|-------------|
| disabled_button | 0/2 (0%) | 2 | 0 | 4.5 | 4.5 |
| escalation_repeat | 0/2 (0%) | 2 | 0 | 4.0 | 6.0 |
| find_element_loop | 0/2 (0%) | 2 | 0 | 0.5 | 2.5 |
| marathon_no_done | 0/2 (0%) | 2 | 0 | 2.0 | 0.5 |
| text_as_toolcall | 1/2 (50%) | 1 | 0 | 6.0 | 5.5 |

## Failed Cases (9 total)

### disabled-button-001

- **Pathology**: disabled_button
- **Status**: fail
- **Query**: Objective: Solve the puzzle to go to step 21
Success criteria: The user goal is completed.

Planner assumptions (validate against current page before acting):
- No explicit assumptions from planner.

Handoff context:
- Planner (planner): Router: agent (User needs to interact with the page (solve a puzzle) to progress, which requires tool actions rather than just reading content.)
- Executor start (executor): Executor started objective: Solve the puzzle to go to step 21

Global task context:
No sibling node context yet.

Reality check signal:
No assumption drift evaluation available.

Execution policy:
- Validate planner assumptions against current page and adjust steps if reality changed.
- Continue from prior context; do not repeat completed work.
- Use global context to avoid duplicating sibling node outcomes.
- If verifier requested reroute/retry, adapt strategy before acting.
- Call done() only when success criteria are satisfied.
- Call done() only when success criteria are satisfied.
- **URL**: https://serene-frangipane-7fd25b.netlify.app/step20?version=1
- **Expected**: inspect_hidden({"pattern":"[A-Z0-9]{6}"})
- **Actual**: click_element({"id":47})
- **Scores**: names=0.00 params=0.00 seq=0.00
  - **Judge reasoning**: The agent saw a pre-filled code '47NV6U' in the input and clicked Submit, which is a reasonable action but misses the key insight: the expected behavior was to use inspect_hidden to find the correct code among many invisible buttons (pathology: disabled_button). The puzzle likely requires identifying which hidden button is the real 'Next' button, not just submitting the pre-filled code. The agent didn't investigate the suspicious pattern of 22 buttons with invisible text before acting.
  - **Scores**: toolSel=4 paramAcc=7 eff=5 antiPat=6 reasoning=5
  - **Prompt fix**: Add a rule: 'When you see multiple buttons with [invisible-text] labels, use inspect_hidden() to examine their properties before clicking any visible submit button. Hidden or invisible elements may contain the actual puzzle solution or navigation path.'

### disabled-button-002

- **Pathology**: disabled_button
- **Status**: fail
- **Query**: Objective: Solve the puzzle to go to step 21
Success criteria: The user goal is completed.

Planner assumptions (validate against current page before acting):
- No explicit assumptions from planner.

Handoff context:
- Planner (planner): Router: agent (User needs to interact with the page (solve a puzzle) to progress, which requires tool actions rather than just reading content.)
- Executor start (executor): Executor started objective: Solve the puzzle to go to step 21

Global task context:
No sibling node context yet.

Reality check signal:
No assumption drift evaluation available.

Execution policy:
- Validate planner assumptions against current page and adjust steps if reality changed.
- Continue from prior context; do not repeat completed work.
- Use global context to avoid duplicating sibling node outcomes.
- If verifier requested reroute/retry, adapt strategy before acting.
- Call done() only when success criteria are satisfied.
- Call done() only when success criteria are satisfied.
- **URL**: https://serene-frangipane-7fd25b.netlify.app/step20?version=1
- **Expected**: escalate({"reason":"Code rejected by form 3 times - need different approach"})
- **Actual**: click_element({"id":47})
- **Scores**: names=0.00 params=0.00 seq=0.00
  - **Judge reasoning**: The agent clicked the Submit Code button, which is a reasonable first action if the code hasn't been submitted yet. However, the expected behavior was to escalate, suggesting the code had already been rejected multiple times. The agent failed to recognize the pattern of prior failures (the pathology tag is 'disabled_button' and the expected action is escalate after 3 rejections), indicating it didn't properly track prior context or recognize it was in a retry loop. The agent's reasoning was shallow - it just narrated the action without considering whether this approach had already failed.
  - **Scores**: toolSel=5 paramAcc=8 eff=4 antiPat=3 reasoning=4
  - **Prompt fix**: Add to the Rules section: 'Before submitting a form or clicking a button, check the handoff context and prior turn history for evidence that this exact action was already attempted and failed. If the same action has failed 2+ times, do NOT repeat it - instead call escalate() with a description of what was tried and what happened, so a different strategy can be planned.'

### escalation-repeat-001

- **Pathology**: escalation_repeat
- **Status**: fail
- **Query**: Objective: ok, find the code to move to the next step and move to the next step
Success criteria: The user goal is completed.

Planner assumptions (validate against current page before acting):
- No explicit assumptions from planner.

Handoff context:
- Planner (planner): Router: agent (User wants to locate and activate the navigation element to proceed to the next step, requiring a click or similar interaction rather than just reading page content.)
- Executor start (executor): Executor started objective: ok, find the code to move to the next step and move to the next step

Global task context:
No sibling node context yet.

Reality check signal:
No assumption drift evaluation available.

Execution policy:
- Validate planner assumptions against current page and adjust steps if reality changed.
- Continue from prior context; do not repeat completed work.
- Use global context to avoid duplicating sibling node outcomes.
- If verifier requested reroute/retry, adapt strategy before acting.
- Call done() only when success criteria are satisfied.
- Call done() only when success criteria are satisfied.
- **URL**: https://serene-frangipane-7fd25b.netlify.app/step1?version=1
- **Expected**: execute_js({"code":"document.querySelectorAll('[data-code],[aria-label]').length"})
- **Actual**: inspect_hidden({"pattern":"code"})
- **Scores**: names=0.00 params=0.00 seq=0.00
  - **Judge reasoning**: The agent used inspect_hidden which is a reasonable approach to find hidden codes, but the expected solution was execute_js to query DOM attributes like data-code and aria-label. The hint on the page explicitly says 'Check the element attributes, aria labels, or meta tags', which should have guided the agent toward a JavaScript DOM query approach rather than a generic pattern search. The agent didn't show reasoning text explaining why it chose inspect_hidden over execute_js.
  - **Scores**: toolSel=5 paramAcc=6 eff=6 antiPat=7 reasoning=4
  - **Prompt fix**: Add to the system prompt: 'When a page hint mentions checking element attributes, aria labels, or meta tags to find hidden codes, use execute_js to query the DOM directly (e.g., document.querySelectorAll("[data-code],[aria-label]")) rather than inspect_hidden. Always read page hints carefully and map them to the most targeted tool.'

### escalation-repeat-002

- **Pathology**: escalation_repeat
- **Status**: fail
- **Query**: Objective: ok, find the code to move to the next step and move to the next step
Success criteria: The user goal is completed.

Planner assumptions (validate against current page before acting):
- No explicit assumptions from planner.

Handoff context:
- Planner (planner): Router: agent (User wants to locate and activate the navigation element to proceed to the next step, requiring a click or similar interaction rather than just reading page content.)
- Executor start (executor): Executor started objective: ok, find the code to move to the next step and move to the next step

Global task context:
No sibling node context yet.

Reality check signal:
No assumption drift evaluation available.

Execution policy:
- Validate planner assumptions against current page and adjust steps if reality changed.
- Continue from prior context; do not repeat completed work.
- Use global context to avoid duplicating sibling node outcomes.
- If verifier requested reroute/retry, adapt strategy before acting.
- Call done() only when success criteria are satisfied.
- Call done() only when success criteria are satisfied.
- **URL**: https://serene-frangipane-7fd25b.netlify.app/step1?version=1
- **Expected**: xray_page({})
- **Actual**: execute_js({"code":"(function() {\n  // Find all elements with data-code attribute\n  const elementsWithCode = document.querySelectorAll('[data-code]');\n  const results = [];\n  elementsWithCode.forEach((el, i) => {\n    results.push(`Element ${i}: ${el.tagName}, data-code=\"${el.getAttribute('data-code')}\"`);\n  });\n  if (results.length === 0) {\n    // Try other common data attributes\n    const allElements = document.querySelectorAll('*');\n    const codeAttrs = ['data-secret', 'data-hidden', 'data-value', 'data-answer', 'data-key'];\n    allElements.forEach(el => {\n      codeAttrs.forEach(attr => {\n        if (el.hasAttribute(attr)) {\n          results.push(`${el.tagName} ${attr}=\"${el.getAttribute(attr)}\"`);\n        }\n      });\n    });\n  }\n  return results.length > 0 ? results.join('\\n') : 'No code attributes found';\n})()"})
- **Scores**: names=0.00 params=0.00 seq=0.00
  - **Judge reasoning**: The expected action was xray_page() to inspect hidden DOM elements, but the agent jumped directly to execute_js with a complex script. The page already shows hints about checking element attributes/aria labels, and the correct approach is to use xray_page to inspect the DOM structure before writing custom JS. The agent skipped the simpler, more appropriate tool in favor of a complex multi-step JS approach.
  - **Scores**: toolSel=3 paramAcc=6 eff=3 antiPat=5 reasoning=4
  - **Prompt fix**: Add to the Rules section: 'When the page mentions hidden elements, codes, or DOM challenges, always call xray_page() first to inspect the full DOM structure before attempting execute_js. xray_page() is the preferred tool for discovering hidden attributes, aria labels, and meta tags.'

### find-element-loop-001

- **Pathology**: find_element_loop
- **Status**: fail
- **Query**: Objective: Enter the code and then hit enter
Success criteria: The user goal is completed.

Planner assumptions (validate against current page before acting):
- No explicit assumptions from planner.

Handoff context:
- Planner (planner): Router: agent (User requests an interactive action: entering a code and submitting, which requires a tool call to fill a field and press enter.)
- Executor start (executor): Executor started objective: Enter the code and then hit enter

Global task context:
No sibling node context yet.

Reality check signal:
No assumption drift evaluation available.

Execution policy:
- Validate planner assumptions against current page and adjust steps if reality changed.
- Continue from prior context; do not repeat completed work.
- Use global context to avoid duplicating sibling node outcomes.
- If verifier requested reroute/retry, adapt strategy before acting.
- Call done() only when success criteria are satisfied.
- Call done() only when success criteria are satisfied.
- **URL**: https://serene-frangipane-7fd25b.netlify.app/step5?version=2
- **Expected**: click_element({"id":392})
- **Actual**: (none)
- **Scores**: names=0.00 params=0.00 seq=0.00
  - **Judge reasoning**: The agent identified the code 'S5CRFE' in the visible elements but then decided to look for an input field instead of acting on the visible span [392] which was the correct target. The expected action was click_element on [392], but the agent called no tools at all, instead narrating intent to read the page. This is a classic anti-pattern of narrating without acting and failing to use visible tagged elements directly.
  - **Scores**: toolSel=0 paramAcc=0 eff=0 antiPat=2 reasoning=3
  - **Prompt fix**: Add to the Rules section: 'If the task involves entering/submitting a code and a span or div element contains that code text with a visible tag [N], click it directly — do not search for an input field first. Treat clickable code elements as the submission mechanism when no input field is explicitly visible.'

### find-element-loop-002

- **Pathology**: find_element_loop
- **Status**: fail
- **Query**: Objective: Enter the code and then hit enter
Success criteria: The user goal is completed.

Planner assumptions (validate against current page before acting):
- No explicit assumptions from planner.

Handoff context:
- Planner (planner): Router: agent (User requests an interactive action: entering a code and submitting, which requires a tool call to fill a field and press enter.)
- Executor start (executor): Executor started objective: Enter the code and then hit enter

Global task context:
No sibling node context yet.

Reality check signal:
No assumption drift evaluation available.

Execution policy:
- Validate planner assumptions against current page and adjust steps if reality changed.
- Continue from prior context; do not repeat completed work.
- Use global context to avoid duplicating sibling node outcomes.
- If verifier requested reroute/retry, adapt strategy before acting.
- Call done() only when success criteria are satisfied.
- Call done() only when success criteria are satisfied.
- **URL**: https://serene-frangipane-7fd25b.netlify.app/step6?version=2
- **Expected**: type_text({"id":1,"text":"code","pressEnter":true})
- **Actual**: press_key({"key":"a","modifiers":["ctrl"]})
- **Scores**: names=0.00 params=0.00 seq=0.00
  - **Judge reasoning**: The agent should have used type_text on element [1] (the visible text input with tag [1]) to enter the code and press Enter. Instead, it called press_key with Ctrl+A, which is completely wrong - it doesn't fill the input field, doesn't enter any code, and doesn't submit. The correct action was a single type_text call with pressEnter:true on the visible input element.
  - **Scores**: toolSel=1 paramAcc=1 eff=1 antiPat=3 reasoning=1
  - **Prompt fix**: Add to the Rules section: 'When a form input is visible with a tag [N], use type_text({id: N, text: <value>, pressEnter: true}) to fill and submit it in a single step. Never use press_key as a substitute for type_text when filling form fields. Always identify the target input element from Visible Elements before acting.'

### marathon-done-001

- **Pathology**: marathon_no_done
- **Status**: fail
- **Query**: Objective: Enter the code and then hit enter
Success criteria: The user goal is completed.

Planner assumptions (validate against current page before acting):
- No explicit assumptions from planner.

Handoff context:
- Planner (planner): Router: agent (User requests an interactive action: entering a code and submitting, which requires a tool call to fill a field and press enter.)
- Executor start (executor): Executor started objective: Enter the code and then hit enter

Global task context:
No sibling node context yet.

Reality check signal:
No assumption drift evaluation available.

Execution policy:
- Validate planner assumptions against current page and adjust steps if reality changed.
- Continue from prior context; do not repeat completed work.
- Use global context to avoid duplicating sibling node outcomes.
- If verifier requested reroute/retry, adapt strategy before acting.
- Call done() only when success criteria are satisfied.
- Call done() only when success criteria are satisfied.
- **URL**: https://serene-frangipane-7fd25b.netlify.app/step8?version=2
- **Expected**: escalate({"reason":"Exceeded 50 turns without completing objective"})
- **Actual**: click_element({"id":512})
- **Scores**: names=0.00 params=0.00 seq=0.00
  - **Judge reasoning**: The agent was in a marathon loop (pathology tag: marathon_no_done) that had exceeded 50 turns without completing the objective. The correct action was to call escalate() with a reason about exceeding turn limits. Instead, the agent clicked element [512] (one of the many invisible/decoy buttons) without any visible reasoning, showing no observe→reason→act logic and continuing the anti-pattern of cycling without resolution.
  - **Scores**: toolSel=1 paramAcc=1 eff=1 antiPat=0 reasoning=0
  - **Prompt fix**: Add an explicit turn-count check rule: 'If you have been attempting the same objective for more than 15 turns without measurable progress toward success criteria, call escalate({"reason": "Exceeded maximum attempts without completing objective"}) immediately. Do not continue clicking elements or retrying failed approaches indefinitely.'

### marathon-done-002

- **Pathology**: marathon_no_done
- **Status**: fail
- **Query**: Objective: Solve the puzzle to go to step 21
Success criteria: The user goal is completed.

Planner assumptions (validate against current page before acting):
- No explicit assumptions from planner.

Handoff context:
- Planner (planner): Router: agent (User needs to interact with the page (solve a puzzle) to progress, which requires tool actions rather than just reading content.)
- Executor start (executor): Executor started objective: Solve the puzzle to go to step 21

Global task context:
No sibling node context yet.

Reality check signal:
No assumption drift evaluation available.

Execution policy:
- Validate planner assumptions against current page and adjust steps if reality changed.
- Continue from prior context; do not repeat completed work.
- Use global context to avoid duplicating sibling node outcomes.
- If verifier requested reroute/retry, adapt strategy before acting.
- Call done() only when success criteria are satisfied.
- Call done() only when success criteria are satisfied.
- **URL**: https://serene-frangipane-7fd25b.netlify.app/step20?version=1
- **Expected**: escalate({"reason":"Stuck on step 20 for 50+ turns, code keeps being rejected"})
- **Actual**: type_text({"id":40,"text":"4T9589","pressEnter":false})
- **Scores**: names=0.00 params=0.00 seq=0.00
  - **Judge reasoning**: The agent is in a marathon_no_done pathology - it has been cycling for 50+ turns trying random codes without success, yet continues to attempt another code guess instead of escalating. The expected behavior was to call escalate() recognizing it is stuck. The agent shows no reasoning text at all, blindly typing another code attempt without any observe-think-act cycle, violating core loop requirements. This is a clear anti-pattern of repeating failed approaches without switching strategy.
  - **Scores**: toolSel=3 paramAcc=2 eff=2 antiPat=1 reasoning=1
  - **Prompt fix**: Add an explicit rule: 'If you have attempted the same type of action (e.g., entering codes) more than 5 times without progress toward the success criteria, you MUST call escalate() with a description of what you tried and why you are stuck. Do not continue cycling through variations of the same failed approach.'

### text-toolcall-001

- **Pathology**: text_as_toolcall
- **Status**: fail
- **Query**: Objective: ok, you are now on step 9. Find a solution to go to step 10 and then STOP there and verify you are on step 10
Success criteria: The user goal is completed.

Planner assumptions (validate against current page before acting):
- No explicit assumptions from planner.

Handoff context:
- Planner (planner): Router: agent (The user requests a simple navigation action (move from step 9 to step 10) and verification, which can be accomplished with a few tool calls (click and read). This fits the 'agent' cate
- Executor start (executor): Executor started objective: ok, you are now on step 9. Find a solution to go to step 10 and then STOP there and verify you are on step 10

Global task context:
No sibling node context yet.

Reality check signal:
No assumption drift evaluation available.

Execution policy:
- Validate planner assumptions against current page and adjust steps if reality changed.
- Continue from prior context; do not repeat completed work.
- Use global context to avoid duplicating sibling node outcomes.
- If verifier requested reroute/retry, adapt strategy before acting.
- Call done() only when success criteria are satisfied.
- Call done() only when success criteria are satisfied.
- **URL**: https://serene-frangipane-7fd25b.netlify.app/step9?version=2
- **Expected**: click_element({"id":3})
- **Actual**: click_element({"id":390}), click_element({"id":589}), click_element({"id":3}), read_page({}), find_element({"text":"code"}), read_page({}), find_element({"text":"Enter Code"}), find_element({"text":"Submit"}), click_element({"id":75}), read_page({}), read_page({}), find_element({"text":"Step 10"}), scroll_page({"direction":"down"}), read_page({}), find_element({"text":"Step 10"}), click_element({"id":337}), read_page({}), find_element({"text":"Step 10"}), click_element({"id":442}), read_page({}), find_element({"text":"Step 10"}), find_element({"text":"input"}), find_element({"text":"Submit Code"}), xray_page({}), click_element({"id":75}), read_page({}), find_element({"text":"Step 10"}), scroll_page({"direction":"down"}), scroll_page({"direction":"down"}), read_page({}), find_element({"text":"Next"}), click_element({"id":75}), read_page({}), find_element({"text":"Step 10"}), inspect_hidden({"pattern":"[0-9]{4,}"}), execute_js({"code":"(function(){return Array.from(document.querySelectorAll('input')).map(i=>({id:i.id,placeholder:i.placeholder,value:i.value}));})()"}), read_page({}), scroll_page({"direction":"down"}), read_page({}), find_element({"text":"Continue"}), click_element({"id":181}), read_page({}), find_element({"text":"Step 10"}), find_element({"text":"Move On"}), find_element({"text":"Proceed"}), scroll_page({"direction":"bottom"}), read_page({}), find_element({"text":"Step 10"}), execute_js({"code":"window.location.href"}), read_page({}), inspect_hidden({"pattern":"code"}), find_element({"text":":"}), execute_js({"code":"(function(){let els=document.querySelectorAll('div, span, p'); for(let el of els){if(/\\d{4,}/.test(el.textContent)){return el.textContent;}} return null;})()"}), read_page({}), execute_js({"code":"(function(){return document.querySelectorAll('input').length;})()"}), read_page({}), click_element({"id":75,"count":2}), read_page({}), find_element({"text":"Step 10"}), click_element({"id":119}), read_page({}), find_element({"text":"Step 10"}), click_element({"id":444}), read_page({}), find_element({"text":"Step 10"}), find_element({"text":"Continue"}), inspect_hidden({"pattern":"Next"}), execute_js({"code":"(function(){let btns=document.querySelectorAll('button'); for(let b of btns){if(b.textContent.includes('Next Page')){return b.getAttribute('data-id')||b.id||'no-id';}} return null;})()"}), read_page({}), find_element({"text":"Next Page"}), click_element({"id":3}), inspect_hidden({"pattern":"\\d{4,}"}), execute_js({"code":"(function(){return Array.from(document.querySelectorAll('div, span, p')).map(el=>el.textContent).join('||');})()"})
- **Scores**: names=1.00 params=0.00 seq=0.01
  - **Judge reasoning**: The correct action was a single click on element [3] (the 'Reveal Code' button), but the agent made 70+ tool calls including clicking non-existent elements (id 390, 589), repeatedly calling read_page and find_element in cycles, and never successfully completing the task. The agent exhibited severe anti-patterns: repeating failed calls, cycling without progress, and using find_element when visible tags were available. The agent also demonstrated the 'text_as_toolcall' pathology by outputting tool calls as text in some instances.
  - **Scores**: toolSel=2 paramAcc=2 eff=1 antiPat=1 reasoning=2
  - **Prompt fix**: Add an explicit rule: 'When multiple buttons are invisible (white text on white background), first click the most semantically relevant visible button (e.g., Reveal Code) before attempting any other navigation. Never call find_element for elements already listed with visible [N] tags. If you have made more than 5 tool calls without a URL change or clear progress signal, stop and call done() with an escalation message rather than continuing to cycle.'

## Prompt Improvement Recommendations

1. **[LOW]** (disabled_button, 1x) Add a rule: 'When you see multiple buttons with [invisible-text] labels, use inspect_hidden() to examine their properties before clicking any visible submit button. Hidden or invisible elements may contain the actual puzzle solution or navigation path.'
2. **[LOW]** (disabled_button, 1x) Add to the Rules section: 'Before submitting a form or clicking a button, check the handoff context and prior turn history for evidence that this exact action was already attempted and failed. If the same action has failed 2+ times, do NOT repeat it - instead call escalate() with a description of what was tried and what happened, so a different strategy can be planned.'
3. **[LOW]** (escalation_repeat, 1x) Add to the system prompt: 'When a page hint mentions checking element attributes, aria labels, or meta tags to find hidden codes, use execute_js to query the DOM directly (e.g., document.querySelectorAll("[data-code],[aria-label]")) rather than inspect_hidden. Always read page hints carefully and map them to the most targeted tool.'
4. **[LOW]** (escalation_repeat, 1x) Add to the Rules section: 'When the page mentions hidden elements, codes, or DOM challenges, always call xray_page() first to inspect the full DOM structure before attempting execute_js. xray_page() is the preferred tool for discovering hidden attributes, aria labels, and meta tags.'
5. **[LOW]** (find_element_loop, 1x) Add to the Rules section: 'If the task involves entering/submitting a code and a span or div element contains that code text with a visible tag [N], click it directly — do not search for an input field first. Treat clickable code elements as the submission mechanism when no input field is explicitly visible.'
6. **[LOW]** (find_element_loop, 1x) Add to the Rules section: 'When a form input is visible with a tag [N], use type_text({id: N, text: <value>, pressEnter: true}) to fill and submit it in a single step. Never use press_key as a substitute for type_text when filling form fields. Always identify the target input element from Visible Elements before acting.'
7. **[LOW]** (marathon_no_done, 1x) Add an explicit turn-count check rule: 'If you have been attempting the same objective for more than 15 turns without measurable progress toward success criteria, call escalate({"reason": "Exceeded maximum attempts without completing objective"}) immediately. Do not continue clicking elements or retrying failed approaches indefinitely.'
8. **[LOW]** (marathon_no_done, 1x) Add an explicit rule: 'If you have attempted the same type of action (e.g., entering codes) more than 5 times without progress toward the success criteria, you MUST call escalate() with a description of what you tried and why you are stuck. Do not continue cycling through variations of the same failed approach.'
9. **[LOW]** (text_as_toolcall, 1x) Add an explicit rule: 'When multiple buttons are invisible (white text on white background), first click the most semantically relevant visible button (e.g., Reveal Code) before attempting any other navigation. Never call find_element for elements already listed with visible [N] tags. If you have made more than 5 tool calls without a URL change or clear progress signal, stop and call done() with an escalation message rather than continuing to cycle.'
