# PROFILE.md — your standing profile (canonical)

The OpenClaw brain reads this to personalize every task. It is the **canonical**
home for your profile; the OpenSidebar extension keeps a local copy as a cache.

Edit it directly, or let OpenSidebar project your non-sensitive profile digest
here.

## Identity

- Name:
- Location:
- Timezone:

## Preferences

- (e.g. "apply to senior RN roles in SF", "always preview before submitting",
  "prefer concise summaries")

## Files

- CV / résumé alias: `CV.pdf`  — place the file in `~/opensidebar-workspace/`.

## Sensitive — DO NOT put values here

Sensitive values (government IDs, license/account numbers, secrets) stay
**encrypted in the OpenSidebar extension** and are injected into a single tool
call only with your per-task consent. They are never written to this file or to
OpenClaw's storage. List only the *names* of fields you've stored, if helpful:

- (e.g. "RN license number — stored in OpenSidebar, sensitive")
